namespace LunyScript
{
	/// <summary>
	/// Debugging hooks for execution tracing and breakpoints.
	/// Placeholder for Step 1, full implementation in Step 2.
	/// </summary>
	public sealed class DebugHooks
	{
		// TODO Step 2: Add event hooks
		// public event Action<IBlock, RunContext> OnBlockExecute;
		// public event Action<IBlock, RunContext> OnBlockComplete;

		// TODO Step 2: Add execution tracing
		// public bool EnableTracing { get; set; }
		// public List<ExecutionTrace> Traces { get; }
	}
}
